# Yarn configuration for TI-dashboard
# Equivalent to npm's legacy-peer-deps for dependency resolution

# Ignore peer dependency warnings and conflicts
# This allows installation when peer dependencies don't match exactly
ignore-engines false

# Network timeout for slower connections
network-timeout 600000

# Strict SSL (set to false only if behind corporate proxy)
strict-ssl true
